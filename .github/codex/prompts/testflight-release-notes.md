Use $tauritavern-testflight-notes to inspect the prepared release range, then use $tauritavern-release-humanizer for the final editing pass.

Read `.testflight-notes/release-context.md`, use any maintainer-written release notes to prioritize testing, then verify every included claim against the corresponding Git diff and the authoritative `ios_external_beta` policy. Do not modify the repository. Return only the plain text required by the TestFlight notes skill.
