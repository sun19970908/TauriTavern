// @ts-check

// Virtualization policy constants live above the concrete TanStack adapter so geometry
// policy remains independent from the pinned virtualizer implementation.
export const CHAT_VIRTUAL_ESTIMATE_PX = 200;
export const CHAT_VIRTUAL_OVERSCAN = 2;
export const CHAT_VIRTUAL_MAX_VIEWPORT_ITEMS = 32;
export const CHAT_VIRTUAL_SCROLL_END_THRESHOLD_PX = 5;
export const CHAT_VIRTUAL_SCROLL_RESET_DELAY_MS = 150;
